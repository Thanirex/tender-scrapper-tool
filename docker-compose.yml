services:
  tender-scrapper:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - TENDER_DATA_DIR=/data
    volumes:
      - "~/Documents/Tender Scrapping Documents:/data"
    restart: unless-stopped
