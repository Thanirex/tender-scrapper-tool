services:
  tender-scrapper:
    build: .
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      - TENDER_DATA_DIR=/data
    volumes:
      - tender-data:/data
    restart: unless-stopped

volumes:
  tender-data:
